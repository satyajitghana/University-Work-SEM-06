# BankingApp - A server and client using gRPC and MongoDB

## Build Instructions

see ``build_instruction.md`` for details on installing mongocxx driver

1. ``bazel build //:zirconium_server``
2. ``bazel build //:zirconium_client``
